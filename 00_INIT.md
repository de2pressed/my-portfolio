# INIT - My Portfolio

This root note mirrors the current rewrite baseline.

Current stack:

- `Next.js` App Router
- `Supabase`
- `YouTube IFrame API`
- `Vercel`

Do not reintroduce the old Vite, Express, SQLite, or React Router path.

Key rules:

- Keep `youtubeUrl` as the track source of truth.
- Keep the admin surface hidden behind the `de2pressed` trigger and Supabase auth.
- Keep the loading screen full-screen and frosted.
- Keep the player docked into the footer.
- Keep the version badge in the bottom-left corner and increment it by `0.01` per changelog.

Refer to `README.md` for setup and `my-portfolio-brain/00_PROJECT_BRAIN.md` for the source-of-truth project state.
